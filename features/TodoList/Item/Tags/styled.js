import styled from "styled-components";

export const TagContainer = styled.div`
  display: flex;
  overflow: scroll;
  overflow-x: hidden;
  overflow-y: hidden;
  flex-direction: row;
  flex-wrap: inherit;
`;
