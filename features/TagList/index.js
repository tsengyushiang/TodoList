import Add from "./Add";
import List from "./List";
import Submit from "./Submit";

const TagList = () => {
  return (
    <div>
      <Add />
      <List />
      <Submit />
    </div>
  );
};

export default TagList;
